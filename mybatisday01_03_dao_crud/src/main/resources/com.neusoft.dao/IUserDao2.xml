<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.dao.IUserDao2">

<!--    配置查询结果的列名和实体属性名的对应关系-->
    <resultMap id="userMap" type="uSeR">
<!--        主键字段的对应-->
        <id property="userId" column="id"></id>

        <result property="userName" column="username"></result>
        <result property="userAddress" column="address"></result>
        <result property="userSex" column="sex"></result>
        <result property="userBirthday" column="birthday"></result>

    </resultMap>
    <select id="findById" parameterType="INT" resultType="com.neusoft.domain.User">
        select * from user where id = #{uid}
    </select>

    <select id="findAll" resultMap="userMap" resultType="com.neusoft.domain.User">
        select * from user
    </select>

</mapper>